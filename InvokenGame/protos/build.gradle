apply plugin: "java"

sourceCompatibility = 1.6

dependencies {
    compile fileTree(dir: 'libs', include: '*.jar')
}

sourceSets.main.java.srcDirs = [ "src-gen/" ]

eclipse.project {
    name = appName + "-protos"
}
